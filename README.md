# Commandes de déploiement

## Commandes Git

```bash
git status
git add .
git commit -m "Améliorations de l'assistant YIYI: responsive design, touche Entrée, accessibilité et compatibilité PC"
git push origin main
```

## Commandes Wrangler (Cloudflare Worker)

```bash
cd cloudflare-worker
wrangler deploy
cd ..
```